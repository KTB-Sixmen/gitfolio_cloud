apiVersion: storage.k8s.io/v1
kind: StorageClass
metadata:
  name: gitfolio-metrics-sc
provisioner: ebs.csi.aws.com
parameters:
  type: gp3
  encrypted: "false"
reclaimPolicy: Delete  # PVC 삭제 후 PV 보존 여부 (Retain/Delete)
volumeBindingMode: WaitForFirstConsumer
allowVolumeExpansion: true