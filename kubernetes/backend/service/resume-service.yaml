# resume-service.yaml
apiVersion: v1
kind: Service
metadata:
  name: gitfolio-resume-service
  namespace: gitfolio-back
  labels:
    app: gitfolio-resume
spec:
  selector:
    app: gitfolio-resume
  type: ClusterIP
  ports:
    - name: http
      port: 80            # 파드가 개방하는 포트
      targetPort: 8080    # 앱이 리스닝하는 포트 
      protocol: TCP
    - name: https
      port: 443
      targetPort: 8080
      protocol: TCP