# web1-service.yaml
apiVersion: v1
kind: Service
metadata:
  name: gitfolio-front-service
  namespace: gitfolio
  labels:
    app: gitfolio-front
spec:
  selector:
    app: gitfolio-front
  type: ClusterIP
  ports:
    - name: http
      port: 80            # 파드가 개방하는 포트
      targetPort: 3000    # 앱이 리스닝하는 포트 
      protocol: TCP
    - name: https
      port: 443
      targetPort: 3000
      protocol: TCP